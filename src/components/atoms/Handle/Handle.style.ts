import { css } from '@emotion/react';


export const styles = {
  handle: css({
    width: 16,
    height: 16,
    position: 'absolute',
    top: 500,
    left: 500,
    backgroundColor: 'red',
    boxShadow: '0 0 0 1px rgba(0, 0, 0, 0.25)',
    borderRadius: '50%',
  }),
};
